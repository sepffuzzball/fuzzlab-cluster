---
apiVersion: v1
kind: Service
metadata:
  name: sepf
  namespace: default
spec:
  type: ExternalName
  externalName: 10.0.2.60
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: 10.0.2.60
    external-dns.alpha.kubernetes.io/target: external."${SECRET_DOMAIN}"
  name: proxy-sepf
  namespace: default
spec:
  ingressClassName: sepf
  rules:
    - host: sepf.${SECRET_DOMAIN}
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: sepf
                port:
                  number: 880
  tls:
    - hosts:
        - sepf.${SECRET_DOMAIN}
